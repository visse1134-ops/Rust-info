{
  "name": "systeminfo-webhook",
  "version": "1.0.0",
  "description": "Express server that sends system info to a Discord webhook",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}
